---
hide_table_of_contents: true
---
import Playground from "@src/components/playground";

## 引导设备进入 Bootloader 模式

将设备重新引导至 bootloader 模式。

ES6
```javascript
const result = await OneKeyConnect.bixinReboot(params);
```

CommonJS
```javascript
OneKeyConnect.bixinReboot(params).then(function(result) {

});
```

### 例子
```javascript
OneKeyConnect.bixinReboot();
```

### 结果

```javascript
{
    success: true,
    payload: {
        message: 'device reboot started'
    }
}
```

错误

```javascript
{
    success: false,
    payload: {
        error: string // error message
    }
}
```

<Playground
    initValue="OneKeyConnect.bixinReboot();"
/>
